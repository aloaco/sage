meta {
  name: Chat
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}{{apiPath}}/chat
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "messages": [
      {
        "role": "system",
        "content": "You are a helpful software project consultant."
      },
      {
        "role": "user",
        "content": "What are the key considerations when building a social media platform?"
      }
    ],
    "model": "google/gemini-2.5-flash"
  }
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain data", function() {
    expect(res.getBody()).to.have.property('data');
    expect(res.getBody().data).to.be.a('string');
  });
}